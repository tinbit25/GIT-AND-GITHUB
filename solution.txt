1.
      1.Set Up the Local Repository:
      2.Create a directory LocalRepoProject and a file hello.txt inside it.
      3.Add the content: Hello, Git and GitHub! to hello.txt.
      4.Initialize a Git repository:
      git init
      git branch -M main
     
      5.Add the file to the staging area:
      6.Commit the File:

      git add hello.txt
      git commit -m "Add hello.txt with greeting"
      7.Push to GitHub:
      Create a new repository on GitHub named LocalRepoProject (without initializing it).
      Link the local repository to GitHub:
      
      git remote add origin <REPOSITORY-URL>
      git push -u origin main